{{- if $.Values.svc.ingress.enabled -}}
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: {{ .Values.deployment.projectName }}-grpc
  namespace: {{ .Values.deployment.namespace }}
spec:
{{ toYaml .Values.svc.grpc.backendConfig | indent 2 }}
{{- end -}}